# 読むプ（YomuP）

Web ページおよび PDF での読書を支援する Chrome 拡張機能です。マウスオーバーによる下線ハイライト、文字数（日本文）／語数（英文）カウント、読了時間の目安、読書速度・ライン進行、ストップウォッチ・部分タイマーなどを提供します。

**現在のリリース: 3.8.0**

### 3.8.0（2026-07-31）

- **ハイライト** — phrasing のみの短文 `div`（`<br>` なし）と、`footer`/`header` 直テキストが光らない問題を修正（CO-1 / CO-2）
- **見出し** — `h1` 内の UI（bookmark / LLM tools 等）文言を語数・下線から除外し、ライン進行が極端に遅くなる問題を修正（CP-3）
- **読書速度** — 英文基準を 225語/分 → **90語/分**（500字/分相当）に調整。他プリセットも同比率で換算

### 3.7.0（2026-07-29）

- **愛知総合工科附属中 CMS** — `〇` 箇条書きの `<br>` 行分離（AT-1）、一覧 `dd` 内 `except` の折り返し細切れ・ライン進行遅さ（AT-2）を修正
- **Arduino 教材トップ** — `p.hero-lead` で「できた！」など鉤括弧内の `！` を文末と誤判定して切れる問題を修正（AL-8）

### 3.6.0（2026-07-25）

- **Arduino 教材** — `intro-box` / `guide-card` / `error-item` / `lesson-card` / `step-item` / `li+br` などで光らない・広がる不具合を修正（AL-1〜6）
- **もしも FAQ** — `<p class="answer">` 内の `<br>` 箇条書きが複数行まとめて光る問題を修正（MS-1）
- **JAL Pay** — `h3.ttlLv2`（`stepLabel` + `heading`）の下線 Y ずれを修正（JL-1）
- **Arduino リファレンス** — 目次型 `td` の説明文応答改善（AL-7）、詳細 `td` のソフト折り返し文中切れを修正（AR-1）
- **ローカル HTML** — `content_scripts` の `matches` に `file:///*` を追加（`host_permissions` と整合）

### 3.5.0（2026-06-28）

- **ハイライト改善** — 日経トップのゴースト型カード、CrowdWorks 求人、愛知県公式サイト、MSG 東進、西川ニュース一覧、青空文庫などで光らない／誤って光る不具合を修正
- **見出し・段落** — `h5` / `h6` 見出し、公式 CMS の段落・長文（括弧入り1文）の改善
- **その他** — Ko-fi 料金表示まわりの退行修正、表セル箇条書き・複合リンク行の改善

### 3.4.0（2026-06-26）

- **br-only-div 本文ハイライト** — テキスト + 直下 `<br>` のみの `div` 本文（例: Yahoo!天気・台風概況）を論理塊として下線表示
- **部分タイマー窓** — ダブルクリックで閉じる（砂時計ボタンの点灯も OFF）
- **ライン進行** — 100% 到達後にページをクリックすると 0% から再開（部分タイマーはリセットせず継続）
- **改善** — Ko-fi 料金表示（`H2` + 短文 `div`）、英文の折り返し見出しの下線位置・語数、各種退行修正

### 3.3.0（2026-06-21）

- **任意の開発支援リンク** — 読むプ窓最下部に Ko-fi へのリンクを 1 本追加（`Ko-fiで応援する（任意）`）。寄付は任意で、未寄付でも全機能を無料利用可能。クリック時のみ外部サイト（Ko-fi）が新しいタブで開く

### 3.2.0（2026-06-20）

- **下線ハイライト** — 赤枠から下線表示に変更。読書速度に連動して行順に伸長する **ライン進行**（Web / PDF 共通）
- **読書速度 UI** — 読むプ窓から速度プリセット（150〜1000 字/分）を変更可能
- **ライン進行の一時停止／再開** — ページ上クリックで伸長を止めたり再開
- **部分タイマー** — ライン進行と分離。0 秒以降もマイナスカウント継続。リロード時に窓を復元
- **UI 言語** — コントロール窓・右クリックメニューをブラウザ UI 言語（日本語 / 英語）に連動
- **青空文庫** — `div` + `br` + `ruby` 本文のハイライト。フリガナを文字数・下線から除外
- **短文 leaf `div`** — 英文ドキュメントサイト等でハイライトされない箇所を改善
- **不具合修正** — 手動クローズ後のブラウザ再起動で窓が誤復元される問題を修正

### 3.1.0（2026-06-14）

- **ハイライト予備ボタンの廃止** — 「うまくできない時」用ボタンを削除し、自動判定に統一
- **h4 見出し** — テキスト幅のみ光るよう修正（h1–h3 と同型）

**本リポジトリに含まれるソースコードは、開発者 MZK（以下「作者」）が作成した作品です。**  
ライセンスはリポジトリルートの [LICENSE](./LICENSE)（MIT）に従います。

## Chrome ウェブストア

インストール・概要・更新履歴はストアのページを参照してください。

- [読むプ ： ハイライト・タイマーで集中Web/PDF読書（Chrome ウェブストア）](https://chromewebstore.google.com/detail/%E8%AA%AD%E3%82%80%E3%83%97-%EF%BC%9A-%E3%83%8F%E3%82%A4%E3%83%A9%E3%82%A4%E3%83%88%E3%83%BB%E3%82%BF%E3%82%A4%E3%83%9E%E3%83%BC%E3%81%A7%E9%9B%86%E4%B8%ADweb%E8%AA%AD%E6%9B%B8/ikckgloaoidjoddhldocajagcofeneif?hl=ja)

## プライバシーポリシー

本拡張機能のプライバシーポリシーは、以下の公式 URL にて公開しています。  
Chrome ウェブストアへの登録や、ユーザーへの説明にはこちらを使用してください。

- **[読むプ：プライバシーポリシー](https://mzk-log.github.io/yomup/yomup-privacy-policy.html)**

## 主な機能

### Web ページ

- **マウスオーバーハイライト** — ホバー位置のテキストを読みやすい単位で**下線**表示（日英自動判定。塊の目安は `constants.js` の `MAX_TEXT_LENGTH_FOR_HIGHLIGHT` / `MAX_WORDS_FOR_HIGHLIGHT`）。h1–h4 見出しは見出しテキスト幅のみ。`p` / `li` / カード型 `div` / `br` 区切り本文 `div` 等、サイト構造に応じて自動判定
- **ライン進行** — 読書速度に連動して下線が行順に伸長（→ トグルで ON/OFF）。ページクリックで一時停止／再開。100% 到達後のクリックで 0% から再開
- **読書速度** — 読むプ窓の `<select>` で変更（150 / 250 / 500 / 750 / 1000 字/分。英文は語/分換算）
- **文字数カウント** — ページ全体または選択範囲の文字数（日本文）／語数（英文）
- **読了時間の目安** — 上記読書速度設定に連動
- **ストップウォッチ／部分タイマー** — 読書時間の計測と、ハイライト確定後のカウントダウン（0 秒以降も継続）。部分タイマー窓はダブルクリックで閉じられる
- **UI 言語** — コントロール窓・PDF ツールバー等をブラウザ UI 言語（日本語 / 英語）に連動
- **任意の開発支援** — 読むプ窓最下部から Ko-fi（外部の寄付サイト）へ。応援の有無で機能は変わらない
- **起動** — ツールバーアイコンまたは右クリック「読むプ」

### PDF ファイル（3.0.0以降）

- **専用 viewer** — ブラウザで開いた PDF を、拡張アイコンまたは右クリック「読むプでPDFを開く」から表示（`https://` / `http://` / `file://`）
- **Web 版と同等の支援** — 下線ハイライト、ライン進行（100% 後の再開含む）、読書速度、ストップウォッチ、部分タイマー
- **文字数・読書時間** — 全体および選択範囲（英文は語数）
- **ローカル PDF** — `file://` 利用時は拡張機能の設定で「ファイルの URL へのアクセスを許可」が必要

## 開発者向け：ソースから読み込む

1. Chrome で `chrome://extensions/` を開く  
2. 「デベロッパーモード」をオン  
3. 「パッケージ化されていない拡張機能を読み込む」で、**このフォルダ（`manifest.json` がある階層）** を指定  

※ ストア提出用 ZIP は、このルートの**中身**を zip します（`node_modules/` は含めない。親フォルダを zip のルートにしないこと）。

## 権限（`manifest.json`）

| 権限 | 用途の概要 |
|------|------------|
| `contextMenus` | 右クリック「読むプ」「読むプでPDFを開く」 |
| `activeTab` | アクティブなタブでスクリプト注入・操作 |
| `scripting` | 未注入タブへの動的注入（アイコン・右クリック起動時など） |
| `tabs` | PDF タブ・内蔵 PDF ビューアの判定、viewer への遷移 |
| `host_permissions`（`https://*/*` `http://*/*`） | ユーザー操作時に PDF を fetch して viewer 表示 |
| `host_permissions`（`file:///*`） | ローカル PDF（拡張設定で許可が必要） |

※ `content_scripts` により、Web ページ読込時にも `constants.js` / `ui-strings.js` / `reading-settings.js` / `content.js` を注入します。

## サードパーティ

### Font Awesome（アイコン）

UI の SVG アイコンに **Font Awesome**（無料版）を利用しています。

- [Font Awesome Free のライセンス](https://fontawesome.com/license/free)

Icons by [Font Awesome](https://fontawesome.com), licensed under [CC BY 4.0](https://creativecommons.org/licenses/by/4.0/).

### PDF.js

PDF 表示に **PDF.js**（Mozilla）を同梱しています。帰属表示は [NOTICE](./NOTICE) を参照してください。

- [PDF.js](https://github.com/mozilla/pdf.js) — Apache License 2.0

## フォルダ構成（抜粋）

- **manifest.json** — 拡張機能の定義と権限  
- **background.js** — アイコン・右クリック、PDF fetch / viewer 起動、content 注入  
- **constants.js** — 定数（ハイライト上限、PDF メニュー、寄付リンク URL 等）
- **ui-strings.js** — コントロール窓 UI 文言（日本語 / 英語）
- **reading-settings.js** — 読書速度・ライン進行の永続化
- **_locales/** — 右クリックメニューの i18n（`ja` / `en`）
- **content.js** — Web 版：ハイライト、ポップアップ UI、文字数／読了時間等
- **pdf/** — PDF viewer（`viewer.html` / `viewer.js` / `highlight-core.js` 等）  
- **vendor/** — PDF.js（`pdf.mjs`）、CMap（`cmaps/`）、標準フォント（`standard_fonts/`）  
- **images/** — UI 用 SVG 等  
- **docs/** — プライバシーポリシー・デモ用 HTML  
- **NOTICE** — PDF.js 等の帰属  
- **LICENSE** / **README.md**

## 今後の開発課題

- **PDF 対応**（3.0.0）— 実装済み
- ハイライトがうまくできない箇所に対応する（難易度高。デグレが起きやすい）
    - 2026/5/10 修正実施
    - 2026/5/30 `<dd>` 内の見出しと概要の連結、`pre`（表セル内含む）の1行単位ハイライト等（2.6.0）
    - 2026/5/31 カード型 div、アコーディオン FAQ（2.7.0）
    - 2026/6/6 日本語句読点優先、リスト・nav・表目次列、ニュース記事型 `<p>` 等（2.8.0）
    - 2026/6/14 日経 blockLink 型・PR 誤光り・右空白 hover、Gemini 箇条書き／引用ラベル／タイムライン、h4・予備ボタン廃止（3.1.0）
    - 2026/6/20 青空文庫、developer.chrome 型 leaf `div`、段落内インライン `code`（3.2.0）
    - 2026/6/26 台風概況型 br-only `div` 本文（3.4.0）
    - 2026/6/28 日経ゴーストカード・CrowdWorks・愛知県 CMS・MSG・西川一覧・青空退行修正等（3.5.0）
    - 2026/7/25 Arduino 教材・リファレンス、もしも FAQ、JAL Pay 見出し下線、file:// content script（3.6.0）
    - 2026/7/29 愛知 CMS（〇 br 行・except 折り返し）、Arduino hero-lead 鉤括弧内 `！`（3.7.0）
- tooltip の座標調整 — 2026/5/16 修正実施
- 英文ハイライト — 2026/5/24 対応（2.5.0）
- 読むプ窓がブラウザ縮小で画面外 — 2026/5/30 修正（2.6.0）
- UI の変更としてハイライトを強調する — 2026/6/20 下線・ライン進行（3.2.0）
- 読むプ窓の横長/縦長切り替え（難易度中）

## ライセンス

MIT License — 詳細は [LICENSE](./LICENSE) を参照してください。

## 作者・著作権

Copyright (c) 2026 MZK
